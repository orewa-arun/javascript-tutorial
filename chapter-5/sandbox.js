const age = 21;

// Note that == loose comparison doesn't care about type compared
console.log(age == "21");

// === strict comparison cares about type
console.log(age === "21");
console.log(age !== 21);
console.log(age === 21);